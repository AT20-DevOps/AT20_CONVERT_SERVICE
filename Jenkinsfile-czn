
pipeline{
    agent any
    environment {
        DOCKER_PASS =  credentials('docker_pass')
    }
    stages{
        stage("Test"){
            agent { docker 'node:18-alpine3.16' }
            steps{
                //sh 'npm version'
                //sh 'ls -la'
                sh 'npm install --save-dev ts-jest'
                sh 'npm install --save-dev @types/jest'
                sh 'npx jest'
            }
            post {
                always {
                    archiveArtifacts artifacts: 'test/report/report.html', followSymlinks: false
                }
            }
        }
        stage ('Package'){
            steps {
                sh 'docker build -t at20_convert_service .'
            }
        }
        stage ('Publish'){
            steps {
                sh 'docker login -u carolina01 -p ${DOCKER_PASS}'
                sh 'echo docker tag at20_convert_service carolina01/at20_convert_service'
                sh 'echo docker push carolina01/at20_convert_service'
            }
        }
    }
}